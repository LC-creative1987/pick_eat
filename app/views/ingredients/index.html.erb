<div class="container">
  <div class="restaurants">
    <div class="row">
      <% @ingredients.each do |ingredient| %>
      <div class="col-12 col-md-6 col-sm-12 col-lg-3">
        <div class="card mb-3 rounded">
          <div class="d-flex justify-content-between p-3">

            <div>
              <p class="text-bold"><%= ingredient.name %></p>
              <p class="text-bold"><%= ingredient.unit %></p>
              <p class="text-bold"><%= ingredient.change_increment %></p>
              <p class="text-bold"><%= ingredient.cost %></p>
              <p class="text-bold"><%= ingredient.price %></p>
            </div>

              <div class="d-flex flex-column justify-content-end align-items-end">

                </span>
              </div>

            </div>
          </div>
        </div>

    <% end %>
    </div>
      <%= link_to "Back to the restaurant", restaurant_path(@restaurant), class: "btn btn-primary" %>

  </div>
</div>

